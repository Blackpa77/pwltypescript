function hello(nama: string, umur: number): string {
    return `selamat datang sayang ${nama} di pwl, umur saya ${umur}`;
}
const mahasiswa: string = "Paundra";
const umur: number = 20;
console.log(hello(mahasiswa, umur));